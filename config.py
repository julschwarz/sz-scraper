FILENAME = "ARTICLES.csv" # the name of the file to save the data to
USE_EXISTING_FILE = False # if True, the script will use the existing file and append new rows to it
KEYWORDS = "Flüchtlinge|Geflüchtete" # the keywords to search for in the articles (regular expressions, e.g. "AfD|Alternative für Deutschland") are allowed

START_DATE = "2018-09-03" # the start date of the search
END_DATE = "2018-09-22" # the end date of the search
